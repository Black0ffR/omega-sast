'use strict';

/** @module ast/summaries */
const m = require('./_monolith');
module.exports = {
  computeFunctionSummaries: m.computeFunctionSummaries,
  buildBackwardSlices: m.buildBackwardSlices,
};
